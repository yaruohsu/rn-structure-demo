import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    textInput: {
        height: 200,
        borderRadius: 5,
    },
    idLabel: {
        color: 'red',
    },
    titleLabel: {
        color: 'blue',
        fontSize: 20,
    },
});